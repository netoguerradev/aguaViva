(function() {
  function logError(error) {
    if (console && console.error) {
      console.error(error);
    } else if (console && console.log) {
      console.log(error);
    }
  }

  try {
    var gaKey = window["GoogleAnalyticsObject"] || "ga";
    var ga = window[gaKey];
    if (ga) {
      if (typeof ga.getAll === "function") {
        ga.getAll().forEach(function(tracker) {
          tracker.send("event", {
            eventCategory: "SegmentMatches",
            eventAction: "ClearbitSegmentMatch",
            eventLabel: "0c2b7611-5a01-4a61-a80c-d955e4c16d8d",
            nonInteraction: true
          });
        });
      } else {
        ga("send", "event", {
          eventCategory: "SegmentMatches",
          eventAction: "ClearbitSegmentMatch",
          eventLabel: "0c2b7611-5a01-4a61-a80c-d955e4c16d8d",
          nonInteraction: true
        });
      }
    }
    if (window.gtag) {
      window.gtag("event", "ClearbitSegmentMatch", {
        event_category: "SegmentMatches",
        event_label: "0c2b7611-5a01-4a61-a80c-d955e4c16d8d"
      });
    }
  } catch (error) {
    logError(error);
  }
})();